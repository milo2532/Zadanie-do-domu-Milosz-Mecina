def is_prime(num):
    if num > 1:
        for i in range(2, num):
            if (num % i) == 0:
                return False
        else:
            return True
    else:
        return False

def check_list(numbers):
    for num in numbers:
        if not is_prime(num):
            return False
    return True
